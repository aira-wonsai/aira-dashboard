{
  "name": "aira-dashboard",
  "version": "1.0.0",
  "description": "Aira's personal dashboard",
  "scripts": {
    "sync": "node scripts/sync-data.js",
    "parse-memory": "node scripts/parse-memory.js",
    "auto-sync": "bash ./scripts/auto-sync.sh",
    "test": "vitest",
    "test:e2e": "playwright test",
    "test:ui": "playwright test --ui",
    "test:visual": "playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "vitest": "^1.3.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
